const styles = theme => ({
    navbarContainer: {
        width: '100%',
        height: '50px',
        padding: '5px 10px',
        marginBottom: '1px solid black',
        display: 'flex',
        justifyContent: 'space-between',
        alignItems: 'center'
    }
});

export default styles;
